[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kairos-assistant"
version = "1.0.0"
description = "Kairos Assistant 2.0"

[tool.setuptools.packages.find]
include = ["core*"]
